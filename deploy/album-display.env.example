# /etc/album-display.env 로 복사한 뒤 실제 값을 채울 것.
# 반드시 chmod 600 — 시크릿이 들어간다. 이 예제 파일에는 실제 값을 적지 말 것.
#
# 주의: systemd EnvironmentFile은 셸이 아니다.
#   - export 를 붙이지 말 것
#   - 값에 따옴표를 두르지 말 것
#   - 주석은 줄 맨 앞에서만

SPOTIPY_CLIENT_ID=
SPOTIPY_CLIENT_SECRET=
SPOTIPY_REDIRECT_URI=http://127.0.0.1:8888/callback
